# 04 · 产品决策记录（用户已拍板 · 单一真相）

> 用途：记录用户**已经拍板**的产品决策，作为防 agent 跑偏的唯一真相。与本文件冲突的旧文档一律以本文件为准（旧文档已移入 `docs/_archive/`）。
> 更新日期：2026-06-16。

## 1 · 已定决策

| 编号 | 决策 | 落地约束 |
|---|---|---|
| **D1 手动修改** | **agent-native 为主 + 关键项加"手动编辑"逃生舱**。默认"说一句就改"，但重要配置(模型/系统提示词/Skill/Source/权限等)提供手动编辑面板。 | 手动编辑**必须写进 craft 现有 `config`/`preferences`/settings RPC 与存储**，**不另起第二套真相**（与"不引入第二套 session"同理）。 |
| **D2 记忆系统** | **全本地分级记忆**（工作/情景/语义/程序四层）+ **用户可查、可删、可导出**。 | 详见 `docs/05-记忆系统方案.md`。接口隔离，写进 craft session/config 真相。OpenClaw/Hermes 仅黑盒借鉴思路，**不拷源码**（非绿灯）。 |
| **D3 首攻功能** | **第一个落地的功能 = 终端 / 本机 CLI Runtime Host**，先做 **P0-B CLI 探测**。 | 详见 `docs/Fleet-功能缺口与技术路线.md` §4.1/§5/§7。目标是先让 craft 能安全发现本机 `claude`、`codex`、`qwen`、`opencode`、`cursor`、Antigravity 的 `agy`、`hermes`、`openclaw`、Grok Build 的 `grok`，并按 AionUi catalog 补齐 Aion CLI、Goose、CodeBuddy、Kimi、Factory Droid、Augment Code、GitHub Copilot、Qoder、Mistral Vibe、Nanobot、Snow；Gemini CLI 当前不再列入内置探测；版本探测失败/超时不等于未安装；内置浏览器 + 选元素 / 标注 / 截图顺延到下一阶段，方案仍保留在 `docs/06-浏览器与网页标注方案.md`。 |
| **D4 账号切换** | **仅做合法多账号 Profile 的干净切换、不丢记录**。 | **明确不做**绕用量限制 / 规避检测的自动轮换（违反各家 ToS、有封号风险，Cockpit 那种斗智斗勇不照抄）。设计要点见 §3。 |
| **D5 多模型融合(Fusion)** | **做成设置项**，默认**关**；可选 **关 / 常开 / 智能开**(队长自动判要不要开)；可配 **融合几个模型、panel 成分、判官/综合器模型、由队长用还是队员用**。 | 详见 `docs/03-Fusion多模型融合方案.md`（含"作为设置项"一节）。 |

## 2 · 战略与顶尖增量（**待你拍板**，我的建议）

这些不是已定决策，是要冲"世界顶尖"建议尽快定的（执行难点登记、防跑偏说明、以及"怎么让软件少更新"的适配层架构，见 `docs/07-难点清单与更新韧性.md`）：

1. **分叉策略（需你拍板）。** 硬分叉 vs 软分叉未定，而"去 Craft 化"(改包名/协议/数据目录/全量中文)会逼你选——**全量改名 = 事实硬分叉**，之后难合 craft 上游(它仍活跃)。**我建议软分叉**：核心包名/协议先不动，品牌只做在用户可见层，增量放独立 package，定期合上游。深度去 Craft 化压到你确定不要上游之后。
2. **评测 + 可观测（建议纳入为贯穿工作流）。** craft 只有单元测试，**无 agent 评测/回归框架，无追踪/回放/成本产品面**。这是顶尖工具的护城河，也是 Fusion/编排能"越用越准"的前提。建议早搭骨架，别拖。
3. **顶尖缺口（列入 P2/P3）。** craft 实测**没有**：computer-use（截屏/点击/视觉操作软件）、容器级沙箱（跑不可信代码）、agent 语音/多模态输入。

## 3 · D4 设计要点（合法多账号 + 其他 CLI 历史归集）

- **凭据 Profile**：每个合法账号一份独立 profile（各自 auth / 历史 / 配置），"激活 = 切指针、**绝不删**"→ 不丢记录。craft 已有基础：`auth/state` + 加密 `credentials.enc` + chatgpt-oauth/claude-token。
- **不退出就能切**：**每个会话注入各自凭据 / HOME（路径隔离）**，而非改一个全局文件 → 不同账号可并行、互不踩。
- **其他 CLI 历史归集**：每个 CLI 一个**只读 adapter**（`~/.claude`、`~/.codex`、Grok 等多为 JSONL/SQLite），汇成统一可搜索浏览器；**只读、绝不写其库**、容错版本化；这些历史可喂给语义记忆(D2)。
- 时序：**非首攻**（首攻是 D3 终端 / CLI Runtime Host）。本节作为后续阶段的设计锚。

## 4 · 决策变更规则

- 改动本文件任何一条决策，需用户明确确认，并同步更新引用它的文档（`AGENTS.md`、`00-执行总纲`、相关规格）。
- 任何执行 agent 发现自己要做的事与本文件冲突 → **停下，以本文件为准**。
