# 32 · 并行开发状态看板与工作令（冲突隔离 · 单一真相）

> 状态日期：2026-06-27
> 作用：让多个工程 Agent **同时开发且互不干扰、不跑偏**。它回答四件事：现在做了什么 / 谁拥有哪些文件 / 按什么波次并行 / 每个任务怎么验收。**套件级（S1–S13）"哪些能并行、按什么波次"见 §7。**
> 口径：只按主线 `work/fresh-base-spine` 的**已提交代码**判断状态；其它 worktree、口头“已完成”都不算主线完成（铁律 23）。
> 配套：契约真相见 `docs/33`（团队）/`docs/31`（动作脊柱）/`docs/30`（架构）；**每个 Agent 的完整提示词见 `docs/34`**。

---

## 0 · 不互相干扰的唯一机制：契约冻结 + 硬文件所有权

并行最大的事故源是**多个 Agent 抢改同一个共享契约文件**（`channels.ts`/`routing.ts`/`dto.ts`/`index.ts`/`channel-map.ts`/`types.ts`/i18n JSON/handler 注册）。本看板用三条铁律消除它：

1. **契约只由 Lead 改（Wave 0）。** 所有跨 Agent 需要的 channel、event、command、type、i18n key、handler 注册，由 **Lead 在 Wave 0 一次性冻结并合入主线**。冻结后这些文件**对并行 Agent 只读**。任何并行 Agent 需要新契约 → 回 Lead，不得自己加字段（否则必冲突 + 漂移）。
2. **每个文件恰好一个所有者。** 见 §3 文件所有权矩阵。Agent 只改自己拥有的文件，**禁改清单是硬约束**。新 handler 文件用「Lead 在 Wave 0 建空壳并注册 → 该 Agent 在 Wave 1 填实现」的**顺序移交**（不是并发同改）：Wave 0 后 Lead 永不再碰该文件。
3. **功能文档不拆两份。** 一个页面或能力只能有一份功能文档，里面同时写 UI、后端、Agent 工具、permission、timeline、设置/i18n 和验收。并行 Agent 可以按文件所有权拆实现，但不能各写各的 UI 方案或服务方案。

只要遵守这三条，N 个 Agent 的 diff 在文件级天然不相交，可无冲突合入。

---

## 1 · 状态标记规则

| 标记 | 含义 |
|---|---|
| ✅ 已落主线 | **已提交到当前主线 `work/fresh-base-spine`**，可作依赖 |
| 🟡 候选/待合入 | 已实现 + typecheck 通过，但**尚未提交主线**；不能当“已落主线”依赖，合入并复核后才升 ✅ |
| 🟨 已有基础 | 部分落地，未成闭环 |
| ⛔ 未开始 | 必须另开任务，不能假装完成 |
| 🧊 冻结收口 | 只修 bug/接线，不扩功能 |
| 🔒 Lead 独占 | 只能 Lead 改，不可拆并行 |
| 🧩 可并行 | 边界清楚，独立 Agent 可做 |

**汇报硬规则**：任何 Agent 汇报完成必须含：worktree、branch、commit、改了哪些文件、没碰哪些禁改文件、跑了哪些验证、是否污染主线（格式见 §6）。缺项不算完成。

---

## 2 · 当前已做 / 没做

| 模块 | 状态 | 说明 |
|---|---|---|
| 干净 craft 基座 | ✅ | `app/` = craft 软分叉；**已同步上游 v0.10.4**（`556c59a`，Pi SDK 0.79.9）；`oss:sync` 工具已落地（未提交）；baseline `a512da7` 见 `docs/09` |
| 动作脊柱 DesignAction/Patch/SessionEvent | ✅ | 瘦桥 + actor + permission + rollback + 6/6 证明测试（commit `66b9383f`），`docs/31` |
| 团队协议/规则/Coordinator 基座 | ✅ | 消息/任务/汇报/投递·运行分离/待审队列/权限分级等基座**已提交**（commit `38144c58` 等） |
| 团队身份双写收敛 | ✅ | 身份定义已回到 Craft 原标签系统：`LabelConfig.kind/systemPromptPreset/permissionProfile` + session `labels`；TeamCoordinator/队长投影从 session labels 派生，不再维护第二套身份真相。 |
| 身份提示词 + 权限配置 + 队长唯一性 | ✅ | 默认标签种子汉化并承载身份；`SessionManager.setSessionLabels` 注入 identity system prompt、应用 permission profile，并保持 `priority`/队长唯一。现有工作区需通过标签设置迁移/编辑已有标签。 |
| 管理 Agent 后端/入口 | ✅ | `ManagerAgentLauncher` + S5 全局专栏已落：所有会话空层显示管理 Agent 专门栏，右下角 mini 可唤起/最小化；首次发送创建 hidden craft session，按管理设置选择模型，注入管理 Agent 专用系统提示词，发送走原 permission/tool/timeline；退出行为支持直接退出/常驻小窗。剩余：真正跨工作区单一全局 session、更多结构化管理动作。 |
| 管理 Agent 分级自动决策 L0-L3 | ✅ | 引擎 `decideAuto` + `manager-decision-service` + `managerDecision` RPC + `ManagerSettingsPage` 已落。接入点包括团队权限与中央 `requestWorkflowPermission`；S6 已把相似记忆注入决策 basis；默认关，不绕过 permission。剩余：更细的偏好/规则解释与回放 UI。详见 `docs/17 §4` |
| 团队读 API（team.GET / 待审队列 / 收件箱） | ✅ | LOCAL_ONLY 通道 + handler + channel-map + electron types 已落，供 A2 直接消费 |
| Agent 参与 session 工具（get_team/send_team_message/assign_team_task/submit_team_report） | ✅ | 已接入 session-tools-core 与 session self-management bindings，Agent 可读团队、发团队消息、分派任务、提交报告 |
| `@`/`/` 输入迁移 | ✅ | S8 已落：`@` 不再弹出 Skill/Source/File；团队名册 `@Agent/@身份` 走现有 team mention 路径；`/` 菜单继续负责 Skill/Source 并插入原 `[skill:...]` / `[source:...]` 执行标记；bundled automations 文档已把旧 `@skill` 示例改为 `/`。剩余：模板入口、更多文档扫尾、本机输入体验验收。 |
| 会话列表团队化 UI | ✅ | S3 已落：模型/API provider 图标或 CLI Runtime 终端图标、团队稳定序号 `G-xx`、身份标签固定顺序（队长 → 其他 identity → 普通标签）、队长会话轻量 accent/ring/Users 图标、Progress 小药丸/卡片。剩余：本机视觉验收。 |
| 全部文件 / Library | 🟨 已有基础 | raw「全部文件」只读浏览已改到默认工作台右侧上下文栏：上方 Progress、下方当前工作区文件列表、右侧按钮整体隐藏/显示；`fs:listEntries` 只读 RPC，点文件走原 `onOpenFile`/`FileViewer` 预览。未做：用户额外素材目录、类型过滤/缩略图、拖入专业工作面、Library 资产层、文件移动/重命名/删除的 permission+timeline。 |
| External Job 模型 | ✅ | S10 已落主线：协议/RPC/handler/electron API + `ExternalJobsSettingsPage` 最小 UI。剩余：真实 BrowserPane 外站提交 provider、repomix/MarkItDown sidecar、更多 job type、工作台入口。 |
| 管理 Agent 右下角入口 / 全局专栏 + 退出行为 | ✅ | S5 已落：右下角 mini + “所有会话”层管理 Agent 专栏共用 hidden craft session，发送走原 permission/tool/timeline；退出行为支持直接退出/常驻小窗，设置页可选。不要占用团队群聊的 SessionList 顶部槽。剩余：跨文件夹单一全局 session 需后续 SessionManager/protocol 级设计。 |
| 设计面 re-base openpencil（S2） | ⛔ | **被许可证绿灯阻塞**，未解锁前不可派 Agent（`docs/31` §11） |
| 上下文/审查 · Memory/Decision | 🧊 | S6/S10 已补后端闭环；治理面继续冻结收口，不扩散 UI。外部审查 UI、真实外站 provider 与成本账本可作为 S10 后续小步接线。 |
| 分层记忆后端 + 设置页 + 输入建议 | ✅ | `memory` 协议/RPC/store + 独立 `MemorySettingsPage` 已落；S6 已补全局/工作区存储拆分、TF-IDF + 3-gram 语义检索、episodic 时间衰减、turn 事实抽取、管理决策依据注入、snippet/suggestion 服务；`MemoryInputSuggestions`/`useMemoryInputSuggestions` 已在 `FreeFormInput` 接线。Agent 工具 `list_memory` / `add_memory` / `delete_memory` 已接同一个 store。剩余：向量库/更稳健抽取、归档 UI、L3 清空/导出确认。详见 `docs/05` |
| 任务进度 Progress 后端/UI | ✅ | `progress_updated` 事件/`setProgress` 命令 + agent 工具 `set_session_progress` + `SessionManager.setSessionProgress` + 持久化已落；会话顶部 Progress 卡与会话行 N/M 小药丸已接当前 session progress。详见 `docs/35` |
| CLI Runtime 后端 + ACP/native 发送 | ✅ 已落主线（commits `94f26416` + `1f9bdf49`） | 基座（catalog/health/RPC/协议）+ ACP 发送链路（`services/acp/`，mock-transport 单测）+ Codex/Claude/Grok/Antigravity one-shot adapter + 会话级 runtime/model 选择 + JSONL 持久化 + `sendMessage` 路由 + 附件硬拒绝 + 进程清理。**未做**：usage/额度采样、真实 CLI smoke、Claude 等 CLI 的 reasoning flag 实机确认 |
| CLI Runtime / 模型选择器 UI | ✅ | 设置页可刷新扫描全部本机 Agent CLI 并自动测试；Gemini 已移除，Antigravity 命令为 `agy`。ACP runtime 显示启用开关并进入聊天选择器；Codex/Claude/Grok/Antigravity native/subscription 通过 one-shot adapter 可进聊天 runtime picker；Qwen/Pi/Cursor Agent/OpenClaw 等未接 adapter 的显示“已检测，待接入”。输入框底部按 `docs/36` 拆为 CLI / 模型 / Token 三按钮，并收敛 CLI 模式文案。剩余：本机视觉验收。 |
| 设置页：CLI / 管理 Agent / 记忆 | ✅ | `CliRuntimeSettingsPage`、`ManagerSettingsPage`、`MemorySettingsPage` 已落并接真实 RPC。AI 设置页连接区复用原 provider/API/OAuth/local model 流程。仍需本机视觉验收。 |
| 上游软分叉同步 | ✅ 已落主线 | `app/scripts/oss-sync.ts` + `.oss-sync-state.json`；三路合并 + `--review` 语义审查 + `--finalize`；已验证 v0.10.4，已提交 `362d02b1` |
| 人类交互终端 PTY | ✅ 已落主线 | `BottomTerminalPanel` + `interactive-terminal-ipc.py` + xterm；独立于 Agent CLI Runtime；未接 permission/timeline（人类本机 shell）；已提交 `362d02b1`，待 A2 视觉验收 |
| Git 审查 | ✅ UI 已接线 | `git-review-service` + RPC + `handlers/rpc/git.ts`；`GitReviewPanel` 挂 `WorkspaceContextSidebar` Git 标签；`GitHubSettingsPage` 已注册 settings + i18n |
| Internal Action 扩展登记 | ✅ 脊柱已登记 | S1 files 四条 + **session/progress/memory/team 共 7 条**（`spine-internal-actions.ts` + executor bridge）；browser/terminal 待后续 |
| Fleet 发布/自动更新 | ✅ 可配置 | `packages/shared/src/fleet-publish.ts` + `electron:dist:*:fleet`；`validate:fleet` 检查 publish 块；默认 URL 仍为 craft 对照 |
| 智能模型路由 + Fusion + 缓存 | ✅ 已落主线 | SessionManager Auto 分支 + orchestrator/fusion/semantic-cache/session-routing-bridge + 设置页/输入 Auto/timeline 组件。已实现：mini 判官、RouteLLM/语义缓存磁盘、Plan Fusion + verification、cascade/偏好信号、git HEAD 缓存失效。已提交 `362d02b1`。未做：rtk/codegraph sidecar 实接、A2/A3 smoke。见 `docs/03` |

---

## 3 · 文件所有权矩阵（硬约束）

> 列 = 文件/目录；值 = 唯一所有者。**未列出的共享契约文件一律 Lead 独占。** Agent 只能改自己名下的；改别人的 = 违规返工。

### 🔒 Lead 独占（按批准任务冻结契约 + 实现硬核）

契约（Wave 0，冻结后只读）：
- `app/packages/shared/src/protocol/{channels,routing,dto,index,team}.ts`
- `app/apps/electron/src/transport/channel-map.ts`
- `app/apps/electron/src/shared/types.ts`
- `app/packages/server-core/src/handlers/rpc/index.ts`（注册）
- `app/packages/server-core/src/handlers/handler-deps.ts`
- `app/packages/shared/src/i18n/locales/*.json`（所有注册语言必须同步，并通过 locale parity；不能只改三种语言）
- 新 handler 只有在对应协议和调用方已确定时才注册；禁止为了并行先造空 channel、空 handler 或假实现

当前硬核实现（Wave 0，Lead 独占）：
- `app/packages/shared/src/labels/{types,storage,resolve,values}.ts` 及测试
- `app/packages/shared/src/protocol/team.ts` 及测试
- `app/packages/server-core/src/services/team-rules-service.ts`（移除身份双写）
- `app/packages/server-core/src/services/team-coordinator.ts`（从 session labels 派生身份）
- `app/packages/server-core/src/sessions/SessionManager.ts`（setLabels 接提示词、permission profile、leader 唯一性）
- session self-management / team 工具中受影响的类型与测试

### 🧩 A1 — `@`/`/` 输入迁移（Lead 标签契约完成后）
- `app/apps/electron/src/renderer/components/app-shell/input/FreeFormInput.tsx`
- `app/apps/electron/src/renderer/components/ui/mention-menu.tsx` 及真实 mention/slash 实现文件（先 `rg` 定位；`skill-mention-menu.tsx` 只是 deprecated 转发；聊天输入当前由 `slash-command-menu.tsx` 承担 Skill/Source）
- `app/apps/electron/src/renderer/components/ui/rich-text-input.tsx`
- shared mentions parser（A1 独占的那一个解析文件，非 protocol 契约）+ 其 `__tests__`
- `app/apps/electron/resources/docs/*` 中 `@skill` 类示例文案
- **禁改**：所有 `protocol/*`、`SessionManager*`、会话列表组件（A2）、i18n JSON、`session-tools-core/tool-defs.ts`

### 🧩 A2 — 会话列表团队化 UI（当前阻塞）
- `app/apps/electron/src/renderer/components/app-shell/{SessionItem,SessionList,SessionBadges,SessionInfoPopover}.tsx`
- 阻塞解除后只允许修改 `SessionItem` / `SessionList` / `SessionBadges` 的显示字段。
- 团队群聊特殊会话项必须先由 Lead 核准真实 session 导航落点，再单独授权文件。
- **禁改**：`protocol/*`、i18n JSON（只用 Lead 加的 key）、输入框文件（A1）、`SessionManager*`、后端 handler

### 🧩 A3 — 全部文件 + Library
- 已落基础：默认工作台右侧上下文栏 `WorkspaceContextSidebar`；上半区 Progress，下半区 `FilesListPanel` 浏览当前工作区文件；`fs:listEntries` 只读扫描；点文件走原 `onOpenFile`/`FileViewer` 预览。左侧不再保留「全部文件」导航按钮。
- 后续仍需：`app/packages/server-core/src/services/file-index.ts`（+ test）、`files-library` handler、用户授权素材目录、类型过滤/缩略图、拖入专业工作面、Library 资产层、文件整理写操作的 permission+timeline。
- **禁改**：团队相关文件、`SessionManager*` 核心、其它 Agent 的 handler；新增共享契约仍由 Lead 收口。

### 🧩 A4 — External Job 模型
- **新建** `app/packages/server-core/src/services/external-job-service.ts`（+ test）
- `app/packages/server-core/src/handlers/rpc/external-job.ts`（Lead Wave 0 建壳，A4 填实现）
- **禁改**：`protocol/*`（只消费 Lead 冻结的 `external-job.ts`）、团队/文件相关文件

### A5 — 管理 Agent 全局专栏（已落 S5，后续仅收口）
- 已落： “所有会话”层的管理 Agent 专栏 + 可选右下角唤起按钮 + 退出行为设置组件。
- 后续：跨文件夹单一全局 session 需要 Lead 重新设计 SessionManager/protocol，不在并行 Agent 文件域内。
- **禁改**：`protocol/*`、`SessionManager*`、其它 Agent 文件

---

## 4 · 波次计划（依赖顺序）

```
Wave 0 (已完成)                    Wave 1 (已完成)                       Wave 2
原 LabelConfig 扩展身份能力      S8: @// 输入迁移                     全部文件 + Library
删除团队身份双写                 S3: 原界面字段增量                    S2 网页文档 / S13 能力装载
setLabels 接提示词/权限/队长     S5/S6/S7/S10 回流主线                 S10 UI / 真实 provider 收口
```

- **Wave 0/Wave 1 已合入主线**；后续并行任务必须基于最新 `work/fresh-base-spine` 开独立 worktree。
- **当前团队线进入收口验收。** LabelConfig/session labels 收敛、提示词注入、permission profile 引用、队长唯一性、S3/S8/S5 已落；下一步以视觉验收和真实路由修 bug 为主，不再扩团队 UI。
- 每个并行 Agent 在**独立 worktree + 独立分支**开发，完成后按 §6 汇报，由 Lead/人类串行合入。

---

## 5 · 最容易跑偏的点（所有 Agent 必读）

1. **不碰共享契约。** 需要新 channel/event/type → 回 Lead，不自己加（§0）。
2. **不建第二套 session/team/store。** 只用 craft `SessionManager`/`SessionEvent`/permission（铁律 13）。
3. **不假执行。** 没接后端的写操作必须 disabled 或明确报错，不能谎称完成（铁律 23）。
4. **`@` 只找人，`/` 才是 Skill/命令/模板。** 不让 `@` 再调 Skill。
5. **队长不是超级权限。** 能分派/汇总/请求审查；不能绕 permission、不能自动同意 L3。最高是管理 Agent，但它也不绕 permission。
6. **投递 ≠ 运行。** 发消息/建任务是 L1 投递；`autoRun` 启动 agent 运行是 L2（`docs/33` §0）。
7. **全部文件是 raw view，不是知识库；Library 才是资产层。**
8. **Warp 只学行为，不抄源码/schema/UI。**

---

## 6 · 汇报格式（缺一不可）

```text
任务名：
worktree：
branch：
commit：
改了哪些文件：
没碰哪些禁改文件：
实现了什么：
没实现什么：
验证命令与结果：（typecheck / 目标测试 / git diff --check）
是否有未提交文件：
是否需要主线处理冲突：
```

验证基线（按改动范围选跑）：
```bash
./scripts/craft.sh run typecheck:all
cd app && bun test <你新增/相关的测试文件>
git diff --check
```

---

## 7 · 套件级并行开发计划（S1–S13）

> 回答："哪些套件可以同时开发、哪些必须排队"。判据有三条：**① 依赖**（要不要等别的套件的承重墙）、**② 文件域不相交**（按 §3 矩阵，互不抢同一文件）、**③ 许可证绿灯**（被阻塞的不排）。
> 共享外壳（`AppShell.tsx`/`TopBar.tsx`/`WorkspaceContextSidebar.tsx`/`PanelStackContainer.tsx`/`SessionList` 顶部槽/`channel-map.ts`/`shared/protocol/*`/`shared/types.ts`/`i18n/*.json`）= **Lead 在 Wave 0 冻结的挂点**，并行 Agent 只新增自己的模块组件文件，不改外壳本体（§0 铁律）。

### 7.1 · 依赖与可并行判定

| 套件 | 依赖 | 文件域（所有者） | 许可证 | 并行判定 |
|---|---|---|---|---|
| **S1** 内部动作注册表 + files 样板 | 无（承重墙） | `internal-action.ts`(契约,Lead) + server-core 注册表/executor + files 写服务 + `files` 面板写入口 | 绿 | 🔒 **必须先行串行落主线**；S2/S12/S13 都等它。**✅ 已落主线（commit `9b95344a`），S1 代码 + dto 事件类型 + typecheck:all 通过 + 31 目标测试通过** |
| **S4** 进度 | 无 | progress 协议/卡片 | 绿 | ✅ 已闭环，仅收口 |
| **S6** 记忆增强（语义/衰减/注入） | 无（后端已落） | `memory` 服务/store/协议 + `MemorySettingsPage` | 绿 | ✅ 已落主线（commit `c0a536d9`）；剩余向量库/归档 UI/输入建议全面接线 |
| **S8** `@`/`/` 输入 | Lead 标签契约 | 输入组件（= 既有 **A1**） | 绿 | ✅ 已落主线（commit `4b2b9bbe`）；剩余模板入口/体验验收 |
| **S3** 团队群聊前端 | 后端已落 | `SessionList/SessionItem/SessionBadges`（= 既有 **A2**） | 绿 | ✅ 已落主线（commit `7ac2d553`）；剩余视觉验收 |
| **S7** CLI 运行时 UI + native adapter | 后端已落 | `cli-runtime` 服务/`CliRuntimeSettingsPage`/runtime 选择器三按钮 | 绿 | ✅ 已落主线（commit `1f9bdf49`）；剩余真实 CLI smoke/usage 采样/flag 实机确认 |
| **S5** 管理 Agent 全局专栏 | 管理后端已落 | "所有会话"专栏 + `ManagerAgentLauncher`（= 既有 **A5**） | 绿 | ✅ 已落主线（commit `e51daf17`）；剩余跨工作区单一全局 session 设计 |
| **S10** AIGC / 外发 Job | External Job 模型 | `external-job-service`/`external-job` handler（= 既有 **A4**） | 绿 | ✅ 已落主线（commit `42b242ae`）+ 设置页 UI；剩余真实 provider/更多 job type |
| **S2** 网页·文档块 编辑（第二样板） | **S1** | web-doc surface + 文档块编辑组件 | 绿 | 🧩 S1 落后可并行（文档域） |
| **S13** 能力装载 / 商城 / 隔离 | **S1** | capability catalog/loadout/runtime 服务 + 商城四页签 UI | 绿 | 🧩 S1 落后可并行（能力域） |
| **S12** 录制 / Skill / 插件 | **S1**(+部分 S13) | recorder/skill-distiller 服务 + Skill 包 UI | 绿 | 🧩 S13 之后可并行（录制域） |
| **S9** 设计画布 | S1 + 绿灯 | 设计 surface | ⛔ openpencil 未绿灯 | ⛔ **不排**（`docs/30 §12`） |
| **S11** 视频剪辑 | S1 + 绿灯 | compositor/timeline | ⛔ opencut-classic 未绿灯 | ⛔ **不排**（`docs/30 §12`） |

### 7.2 · 并行波次（最多 6 路同开，对齐 ≤6 面板）

```
Wave 0  (Lead 串行, 必须先合主线)  ✅ 已完成
  · S1 承重墙落主线（注册表 + executor + permission 改造 + files 第一样板）
  · dto 事件类型修复（file_entry_moved/file_edit_undone/internal_action_invoked）
  · typecheck:all + 31 目标测试全通过
  Commit: 9b95344a on work/fresh-base-spine

Wave 1  (S1 已落, 最多 6 路并行 — 已全部回流主线)  ✅ 已完成
  · S8  @// 输入（commit 4b2b9bbe）        · S3 团队群聊前端（commit 7ac2d553）
  · S6  记忆增强（commit c0a536d9）        · S7 CLI Runtime UI/adapter（commit 1f9bdf49）
  · S5  管理全局专栏（commit e51daf17）    · S10 External Job/AIGC（commit 42b242ae）

Wave 2  (依赖 S1, S1 验收后并行 2 路)
  · B1 = S2  网页·文档块第二样板   · B2 = S13 能力装载/商城/隔离

Wave 3  (依赖 S13)
  · B3 = S12 录制/Skill/插件 闭环   · 创作工作面（画布/AIGC/视频在绿灯后）

不排：S9 设计画布、S11 视频剪辑 — 许可证未绿灯前任何波次都不派 Agent。
```

### 7.3 · 并行硬约束（在 §0/§3/§5 之外，套件维度补充）

1. **S1 不并行、不抢跑。** 它是所有写能力的承重墙；**已落主线**，下游可 `git pull work/fresh-base-spine` 后开工。
2. **一套件一文件域。** Wave 1 六路各自只动表中"文件域"列；跨域需求回 Lead 加契约（铁律 36）。
3. **外壳挂点 Lead 冻。** 多套件都想往右侧 Tool Dock / 中间面板 / 顶栏挂模块 → Lead 在 Wave 0 把挂槽（`TOOL_DEFINITIONS`、面板栈位、TopBar 按钮位）定死，套件只交付自己的模块组件，不改外壳布局逻辑（`docs/00A` 红线）。
4. **资源租约防踩脚。** 同时跑多个 Agent 时，CLI/MCP 每会话独立 profileDir、面板每 Agent ≤3、用户可抢占（D22/D25）；不是物理沙箱，是单进程逻辑隔离。
5. **绿灯优先级不可逾越。** S9/S11 即便文件域独立也不排——许可证闸（规则 4 / `docs/30 §12`）高于并行收益。

---

## 8 · 未完成清单与详细执行计划（2026-06-26 审计）

> 本节是 `docs/00 §2` 的展开版：按**文档-代码同步**核对后的真实缺口。✅ = 已提交主线；🟡 = 工作区有代码未提交；🟨 = 部分闭环；⛔ = 未开始。

### 8.1 · 文档与代码不同步项（已在本轮修正或登记）

| 文档说法 | 代码事实 | 处置 |
|---|---|---|
| `docs/00` 称终端 PTY「未完成」 | PTY 已 wired（未提交） | ✅ `docs/00`/`docs/32` 已改为 🟡 候选 |
| `docs/00` 墓碑称 `docs/09` 已删 | `docs/09` 存在且含 oss-sync 说明 | ✅ 墓碑改为「已复活」 |
| `docs/32` 写 craft v0.10.3 | 已 oss-sync 到 v0.10.4 | ✅ 已更新 |
| Git 审查「可用」 | RPC handler 缺失 | ✅ 本轮补 `handlers/rpc/git.ts` |
| `GitHubSettingsPage` | 已注册 settings + i18n | ✅ B3 完成；本机视觉验收待 A2 |
| `docs/05` 称输入建议「未做」 | `MemoryInputSuggestions` 已接 `FreeFormInput` | ✅ `docs/05`/`docs/32` 已更新 |

### 8.2 · 按波次执行的详细计划

#### 阶段 A — 合入与验证（Lead，1–2 天）

| # | 任务 | 产出 | 验收 |
|---|---|---|---|
| A1 | 提交 oss-sync + v0.10.4 + 终端 PTY + Git RPC | 单 commit 或分两 commit | `typecheck:all`、PTY smoke、`oss:sync --status` up to date |
| A2 | 本机 Electron 视觉验收 | bug 列表 | 见下方 **A2 验收清单**；逐项打勾或记 bug |
| A3 | 真实 CLI smoke | Codex/Claude one-shot 各 1 次 | session timeline 有记录 |

**A2 本机视觉验收清单**（`./scripts/craft.sh run electron:dev`）

| 区域 | 检查项 | 通过标准 |
|---|---|---|
| 右侧上下文栏 | 文件 / Git 标签切换 | Git 标签显示分支、变更列表、diff；双击文件可打开预览 |
| 底部终端 | TopBar 开关 | xterm 面板可开/关/缩放；输入回车有 shell 响应（PTY） |
| 设置 · External Jobs | 创建审查任务 | 列表可见；L3 权限确认后可 queued/run；证据区有 bundleHash |
| 设置 · GitHub | CLI 状态 | `gh auth status` 结果与页面一致；刷新不报错 |
| 设置 · CLI / 记忆 / 管理 Agent | 既有页 | 扫描/列表/保存 RPC 正常，无空白页或 i18n key 裸露 |
| 聊天输入 | CLI / 模型 / Token 三按钮 | 切换 runtime 后发送路径正确；Token 用量可读 |
| 聊天输入 | 记忆建议条 | 输入 ≥2 字且无 `@/#/` 菜单时出现建议；点击填入内容（上限 5 条：snippet 优先 + 语义记忆） |
| 会话 Progress | 右侧栏顶部 | 有 progress 时会话显示 N/M；卡片状态与 session 一致 |
| 团队 `@` | 群聊会话 | `@` 只出 roster；选中后插入 mention |
| 回归 | 发送消息 / 权限弹窗 | 普通 API 会话可收发；L2 写操作仍弹 permission |

#### 阶段 B — 接线闭环（Wave 1 收口，3–5 天）

| # | 任务 | 依赖 | 验收 |
|---|---|---|---|
| B1 | S10 External Job UI | S10 后端 ✅ | ✅ `ExternalJobsSettingsPage` 已注册；可创建/确认/运行/查看 job（provider stub） |
| B2 | Git 审查 UI 接线 | A1 Git RPC | ✅ `GitReviewPanel` 挂右侧栏 Git 标签；diff 可读 |
| B3 | GitHub 设置页 | Lead 冻结 i18n | ✅ `GitHubSettingsPage` + `settings.github.*` 七语 + registry |
| B4 | Internal Action 扩展登记 | S1 ✅ | ✅ session/progress + manager(memory) + team 共 7 条 + executor bridge |
| B5 | 记忆输入建议接线 | S6 ✅ | ✅ `SuggestionService` → `listMemory(suggestion:*)` → `FreeFormInput` 建议条 |
| B6 | Fleet 发布源 | 无 | ✅ `FLEET_ELECTRON_UPDATE_URL` + `electron:dist:*:fleet`；默认仍 craft 对照 |

#### 阶段 C — Wave 2 并行（2 路，各 1 Agent）

| 套件 | 核心交付 | 文件域 | 阻塞 |
|---|---|---|---|
| **S2** 网页·文档第二样板 | web-doc surface internal actions + 文档块编辑最小闭环 | design/web-doc 组件 + registry 条目 | S1 files 样板模式 |
| **S13** 能力装载/商城 | `protocol/capability.ts`（Lead 冻）+ catalog/loadout 服务 + 设置页四页签 | `docs/43` 文件域 | S1 + Lead Wave 0 契约 |

#### 阶段 D — Wave 3 与创作面（许可证后）

| 套件 | 条件 | 说明 |
|---|---|---|
| S12 录制/Skill | S13 后 | `docs/39` |
| S9 设计画布 | openpencil 绿灯 | `docs/31` |
| S11 视频剪辑 | opencut 绿灯 | `docs/30 §12` |
| 无限画布/AIGC/视频工作面 | M1+ | `docs/18` |

### 8.3 · 上游同步运维（每次 craft 发版）

```bash
./源码参考/update_repos.sh                    # 更新参考源
./scripts/craft.sh run oss:sync -- --review     # 语义影响报告
./scripts/craft.sh run oss:sync -- --fetch      # 拉远端（可选）
./scripts/craft.sh run oss:sync                   # 三路合并
# 若有冲突：手动解决 → oss:sync --finalize
./scripts/craft.sh run typecheck:all
```

关注面：Fleet 独有 190+ 文件上游不碰；交集文件见 `docs/09` + `--review` 报告。renderer 层冲突风险高于 `packages/` 后端层。

### 8.5 · 自动化门禁（Lead）

| 命令 | 覆盖 | 不覆盖 |
|---|---|---|
| `./scripts/fleet-verify.sh` | typecheck、i18n parity/sorted、shared 测试、脊柱单测、oss-sync 状态、11 条 internal action、publish 配置 | A2 视觉、A3 CLI smoke、Electron 打包实机 |
| `validate:fleet:fast` | 同上但跳过慢测试 | 同上 |
| `electron:dist:*:fleet` | 打包时注入 `FLEET_ELECTRON_UPDATE_URL` | CDN 上传、notarize、用户端 updater 实机 |

**Phase B（Wave 1 接线）已全部完成**（B1–B6）。**A1 已于 commit `362d02b1` 提交**（路由/Fusion + oss-sync + PTY + Git RPC + workbench）。下一阶段自动化无法替代：**A2/A3 本机验收**、**Phase C**（S2+S13 并行，需 Lead 冻契约）。

### 8.6 · 本机 CLI 并行子 agent + 当前工作令（2026-06-27）

> **口径**：本机 Claude / Grok / Antigravity CLI 并行跑任务，**不是** Fleet 应用内 CLI Runtime。启动器：`scripts/cli-subagents.sh`（`--help` 查模型 flag）。

**默认模型（命令行可改）**

| CLI | 默认模型 | 切换示例 |
|---|---|---|
| Claude Code | `deepseek-v4-pro` | `--claude-model deepseek-v4-pro` |
| Grok Build | `grok-build` | `--grok-model grok-composer-2.5-fast` 或 `--grok-both` |
| Antigravity | `Gemini 3.5 Flash (High)` | `--agy-model "Gemini 3.5 Flash (High)"` |

**当前波次：阶段 A 收口（✅ A1 已合入 `362d02b1`，A2/A3 待验收）**

| # | 负责人 | 任务 | 验收 |
|---|---|---|---|
| A1a | **Lead** | 梳理 A1 commit 范围：oss-sync v0.10.4 + PTY + Git RPC + 路由/Fusion 工作区 + workbench/tool-dock | `fleet-verify` 8/8；`git diff --check` 干净 |
| A1b | **Grok** (`grok-build`) | 跑 `app/packages/server-core` 路由/融合/缓存相关单测，修失败 | 目标 `__tests__/*routing*` `*fusion*` `*semantic*` `*mini-judge*` 全绿 |
| A1c | **Grok-fast** (`grok-composer-2.5-fast`) | i18n parity + 路由设置页 key 扫尾 | `lint:i18n:parity` + `lint:i18n:sorted` |
| A1d | **Claude** (`deepseek-v4-pro`) | `context-shaper.ts`：把 rtk/codegraph 从 `pending` 改为可探测 sidecar 调用骨架（不自动装 hook） | 单测 + typecheck 通过 |
| A1e | **Antigravity** | 同步 `docs/03` 实现状态表与 `docs/00`/`docs/32` 一致 | 文档无互相矛盾 |
| A2 | **人类 + Lead** | Electron 视觉清单 §8.2 | 逐项打勾 |
| A3 | **人类** | 真实 CLI smoke（Codex/Claude one-shot）+ Auto/Fusion API 路径 smoke | timeline 有记录 |

**启动示例**

```bash
cd "/Users/lullwen/Documents/GUI 终端"

# Grok 跑路由单测
./scripts/cli-subagents.sh --only grok --grok-model grok-build -- \
  "在 app/packages/server-core 跑 routing/fusion/semantic/mini-judge 相关 bun test，修全部失败，最后汇报通过的测试文件列表"

# 三路上并行（各不同任务）
./scripts/cli-subagents.sh \
  --claude-model deepseek-v4-pro \
  --grok-model grok-build \
  --agy-model "Gemini 3.5 Flash (High)" \
  -- "context-shaper rtk/codegraph 骨架" "跑 server-core 路由单测" "同步 docs/03 实现状态"
```

**Phase C 排队（A1+A2 通过后）**：Lead 冻 `protocol/capability.ts` → S2 + S13 两路并行。

### 8.4 · 仍明确不做的（防跑偏）

- 不建第二套 session/team/memory store（铁律 13）
- 不换 craft 主浏览器栈（D10）
- 不复制红灯源码（规则 4）
- 人类 PTY 终端不接 Agent 工具表（与 CLI Runtime 分离，`docs/37`）
- 治理面（上下文效率 UI、外部审查大盘）只收口不扩张（`docs/01 §6`）
